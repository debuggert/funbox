import React, { PureComponent } from 'react'

import './cardCat.css';

const CardCat = (props) => {
    const {allEmployess, incEmploess} = props;
    return (
        <div className="card">
            This is card
        </div>
    );
}

export default CardCat;